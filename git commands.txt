For every big feature create a new branch


git checkout master
git pull
git checkout -b <branchname>

// Do coding

git add .
git commit -m "<whatever changes"
git push origin <branchname>


// Goto github and create PR

// Once approved - merge PR

// delete the branch
git branch -D <branchname>



// Second feature - follow same steps

